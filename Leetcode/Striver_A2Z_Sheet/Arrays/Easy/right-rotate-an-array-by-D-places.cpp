#include<bits/stdc++.h>
using namespace std;
// Brute -> One by One Approach O(N.d), O(1)

int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    
    return 0;
}

// Better -> Temporary Array Approach O(N), O(d)

// Optimal -> Reversal algorithm O(N), O(1)